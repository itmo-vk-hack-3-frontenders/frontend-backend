import { axios } from "../axios";

const fetchDevices = async () => {
  const response = axios.get("");
  return;
};

const fetchChart = async () => {
  const response = axios.get("");
  return;
};

export {
  fetchDevices,
  fetchChart,
};
